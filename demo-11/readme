This demo is basically for creating an RDS instance,
where we will create a VPC and two subnets, 1 as public and the other as private
then we will create two security groups and attach it, we will also create two aws ec2 instances ( 1 normal and 1as RDS)
